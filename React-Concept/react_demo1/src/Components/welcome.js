/* Ceate The New Components. */
/* This Is Called A Functional Component. */
/* Component Names In React Should Start With An Uppercase Letter. */

function Welcome() {
  /* The Component Returns JSX. */
  /* JSX (JavaScript XML) Is A Syntax Extension That Looks Like HTML But Is Used In React. */
  return (
    <h2>welcome in my code ....</h2>
  );
}

/* This Line Exports The Welcome Component As The Default Export From The File. */
export default Welcome;